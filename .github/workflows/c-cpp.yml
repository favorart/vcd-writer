name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: build complete
      run: pushd build && [ -f "libvcdwriter.so" ] && [ -f "libvcdwriter.a" ] && [ -f "main" ]
    - name: single test
      run: ./build/main   &&   diff  compare.vcd  dump.vcd
